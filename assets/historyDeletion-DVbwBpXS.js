function a(l){return(Array.isArray(l?.historyDeletionLog)?l.historyDeletionLog:[]).map(e=>{const o=Number(e?.startMs),r=Number(e?.endMs);if(!Number.isFinite(o)||o<=0||!Number.isFinite(r)||r<=o)return null;const t=Number(e?.deletedAtMs);return{startMs:Math.floor(o),endMs:Math.floor(r),deletedAtMs:Number.isFinite(t)&&t>0?Math.floor(t):null}}).filter(Boolean).sort((e,o)=>e.startMs-o.startMs)}function u(l,i,e){const o={windows:[],totalMs:0,coversEntireRange:!1};if(!Number.isFinite(i)||!Number.isFinite(e)||e<=i)return o;const r=a(l).map(s=>({startMs:Math.max(s.startMs,i),endMs:Math.min(s.endMs,e)})).filter(s=>s.endMs>s.startMs);if(r.length===0)return o;const t=[];r.forEach(s=>{const n=t[t.length-1];n&&s.startMs<=n.endMs?n.endMs=Math.max(n.endMs,s.endMs):t.push({...s})});const M=t.reduce((s,n)=>s+(n.endMs-n.startMs),0);return{windows:t,totalMs:M,coversEntireRange:t.length===1&&t[0].startMs<=i&&t[0].endMs>=e}}export{u as c};
